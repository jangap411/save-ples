// check if the passwords if both passwords are the same

const confirmUserPassword = (password1, password2) => {
  if (password1 === password2) {
    console.log("Passwords Match");
  } else {
    console.log("Passwords do not Match");
  }
};
